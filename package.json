{
  "name": "your-own-houdini",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "content:validate": "node scripts/validate-content.mjs",
    "test": "vitest run",
    "build": "npm run content:validate && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@kongyo2/cards-css": "0.5.0",
    "@supabase/supabase-js": "^2.112.4"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "^4.1.11"
  }
}
